streamlit[auth]>=1.42,<2.0
Authlib>=1.3.2,<2.0
httpx>=0.27
itsdangerous>=2.2
# Pin starlette to a version Streamlit's auth code was tested against —
# starlette 1.x changed session-cookie behavior in a way that breaks the
# OAuth state round-trip on Streamlit Cloud (MismatchingStateError).
starlette>=0.40,<1.0
requests>=2.31
fpdf2>=2.7
Pillow>=10.0
gspread>=6.0
google-auth>=2.30
google-api-python-client>=2.140
